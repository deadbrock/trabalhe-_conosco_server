# Configuração Nixpacks para Railway
# Node.js 20 LTS — sem Chromium/Puppeteer

[phases.setup]
nixPkgs = ['nodejs_20']

[phases.install]
cmds = ['npm install --legacy-peer-deps']

[phases.build]
cmds = ['npm run build']

[start]
cmd = 'node dist/index.js'
